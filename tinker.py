import streamlit as st
import pandas as pd
import numpy as np

# st.write("hello world")
# x = st.text_input("favourite place ? ")
# st.write(f"user's fav place is {x}")

# chart_data = pd.DataFrame(
#     np.random.randn(20,3),
#     columns=["a","b","c"]
# )

# st.bar_chart(chart_data)
# st.line_chart(chart_data)

